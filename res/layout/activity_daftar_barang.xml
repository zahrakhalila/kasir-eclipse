<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".DaftarBarang" >

    <TextView
        android:id="@+id/textViewDaftarBrg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />

    <EditText
        android:id="@+id/editTextCari"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textViewDaftarBrg"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/textViewDaftarBrg"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <ListView
        android:id="@+id/ListViewDaftarBrg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/editTextCari"
        android:layout_below="@+id/editTextCari"
        android:layout_marginTop="5dp" >
    </ListView>

</RelativeLayout>